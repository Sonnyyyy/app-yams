import { DoThingDirective } from './do-thing.directive';

describe('DoThingDirective', () => {
  it('should create an instance', () => {
    const directive = new DoThingDirective();
    expect(directive).toBeTruthy();
  });
});
